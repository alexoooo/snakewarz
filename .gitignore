# Gradle
.gradle/
build/

# Kotlin compiler session state
.kotlin/

# IDE
.idea/
*.iml
*.ipr
*.iws

# OS
.DS_Store
Thumbs.db

# Claude Code local session config
.claude/

# :lab's match log. Measurements, not sources: they are reproducible from the command that
# produced them, they grow without bound, and a diff of one says nothing.
.lab/
.lab-abtest.log

# Deliberately NOT ignored:
#   gradle/wrapper/gradle-wrapper.jar  - the wrapper must work on a fresh clone
#   kotlin-js-store/                   - the npm lockfile pins the webpack/karma toolchain
