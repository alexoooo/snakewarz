# Ledger — 2026-07-29 agenda

The coordinator's account of the run. **No agent writes to this file** — agents report, the
coordinator writes. See [`README.md`](README.md) for the convention and
[`../../Research-Process.md`](../../Research-Process.md) for how a session uses it.

Delete this file when [`../2026-07-29_Research-Agenda.md`](../2026-07-29_Research-Agenda.md) closes.

## Phases

| Phase | Agents dispatched | State | Landed in the agenda |
|---|---|---|---|
| P1 Instrument integrity and the cost floor | P1a integrity, P1b cost | **landed** | both halves written up |
| P2 The composed field | P2a instrument prep, P2b the field | **landed** | all three sections written up |
| P3 Adopt | P3a verification, P3b execution | **landed** | verification + execution written up |
| P4 The four residual features | P4 | **landed** | probe, features, field and the stale corrections |
| P5 A rollout policy for `uct` | P5a build+probe, P5b field | **landed** | verdict, exchange rate, and the depth lead |
| P6 A policy-rollout leaf | P6 | **landed** | closed on cost; the depth field written up |
| P7 The third seat | P7a instrument, P7b field | **landed** | the map, both boards, written up |
| P8 Fused bitboard passes | P8 | **landed** | the negative, and the SW-03 rule it earned |

## Running notes

*One block per phase, appended as it runs: what was dispatched, what came back, what was believed and
what was checked. The agenda gets the finding; this gets the account of how it was reached, including
the batches that were wasted and why.*

### P1

Split as the process document prescribes: **integrity** (code and tests, no timing) and **cost**
(paired timings and Chrome), dispatched **sequentially rather than concurrently**. The process
document's reason for running *phases* sequentially — a second Gradle build corrupts every
millisecond in the report — applies just as hard to two agents inside one phase when one of them is
the timing half. So P1a runs to completion first and hands over.

Coordinator's own spot-checks before dispatch, both confirming the agenda's ground truth:
`BotKnob.MAX_PER_BOT = 20` (`BotKnob.kt:276`), and `alphabeta` does read `puct`'s prior *defaults*
(`AlphaBetaBot.kt:242`), so ground truth 5's second-golden claim has the mechanism it names.

P1a's brief carries one deviation from the agenda text: the agenda asks it to fix four sentences,
two of which are in the **closed** 2026-07-28 agenda. A closed agenda is not edited. P1a fixes the
two in `PuctBot.kt` and reports the other two line numbers, and the correction is recorded here and
in this agenda's ground truth 3 instead.

**P1a came back with the phase's shape inverted.** It was dispatched to fix four sentences and two
counts; what it found is that the *claims* were all real and the *citations* were all wrong — a
sentence that was not where the agenda said, a second one the agenda never found
(`LearnedEval.kt:88`), closed-agenda line numbers off by one and by eleven, and a sixth stale count
six lines below one it was sent to fix. Read sceptically per the process document, which is why the
coordinator re-ran the grep: two `BotLadderTest` mentions in `:bots` main sources, both now correct,
confirmed independently.

Accepted its judgment on the reverse-direction guard (recorded in *Open at the close* rather than
built) and on widening by one phrase to fix `LeafEval.kt:23`. Both are the cheap call.

Coordinator's own verification before writing the finding: `git status --short`, the new
`ShippedBudget.kt` read in full, the `MatchSetupTest` pin read in full, and a repo-wide grep
confirming no `BotLadderTest`-thresholds falsehood survives in `:bots`. The remaining mentions at
`MatchSetup.kt:225` and `:235` are the *true* form — the ladder's thresholds really were measured at
1,000 — and were correctly left.

P1b dispatched with P1a's handover folded into its brief: the `[bench]` Chrome path's exact shape,
`TimeCommand`'s `budgets = [allowance, 0]` seating (the hazard is worse than the agenda's box says —
the opponent has *zero* allowance), the flag set, and the `EvaluationCost` inter-session disagreement
it must settle rather than average.

**P1b came back having falsified its own first instrument, which is the reason to believe the second
one.** A JVM sweep timing seven bots in one process gave a self-consistent, every-pass-agreeing set of
ratios that were wrong by 4–5× against three independent references. It caught that, threw it away
and built `AppraisalTape`. An agent that reports the batch it wasted is an agent whose surviving
numbers are worth more, and the process document's "read what comes back sceptically" cuts both ways.

Coordinator's own check before writing the finding: read `AppraisalTape.kt` in full rather than
taking the design on report. The load-bearing property is that the subject is seated at *no* slot and
the line bot draws from a private `SplitMix64`, so a subject drawing a different count of random
values cannot shift the line under itself — that is what makes the ratio about two appraisals and not
two games. Two internal consistency checks corroborate it without being designed to: `puct` bare and
`alphabeta:eval=territory` agree to 5%, and `puct:eval=chamber` and `alphabeta` to 1%. Both pairs
share a leaf and differ in search, so the instrument is reading the leaf.

**The 3.45–4.65× result changes what P2 is for.** It was scoped as a ranking exercise; it is now
also the explanation of why the ranking will disagree with every published table, since those gave
the dearer leaf three to four times the clock. So P2's entrant list gains an **equal-allowance
control rung** (`puct:eval=chamber` at the baseline's own budget) that the agenda did not ask for —
without it the field would rank the configs correctly and be unable to say why history differs, and
the first person to read it would assume the field was wrong.

Two coordinator decisions on P1b's open questions:
- **Both `alphabeta` rungs**, not one. Costing favours `eval=territory` by 3.5–4.4×, but its
  published strength table is at `chamber`; picking on cost alone would settle by assumption the
  thing the field exists to measure.
- **Machine scaling and the single reference line** accepted as stated rather than chased. Ratios
  reproduced to 3% across two independent browser runs, and ratios are what the allowance table is
  made of. Both are recorded in *Open at the close*.

P2 split short-then-long: **P2a** builds the lead-size rider and takes the third board's Chrome
allowance row (P1b's open question 3 — the ratios demonstrably do not transfer between boards), and
hands over to the field agent. Short first, because the rider is a diagnostic the fields will be read
through, and because a code change landing while a three-board batch runs is exactly the collision
the process document warns about.

**Splitting P2 short-then-long paid for itself twice.** The rider was scoped as a tidy-up and came
back having halved the 25 Elo conversion bound; and the third-board sweep found that cost is not
monotone in board size, which would have been discovered by the field agent only as an anomaly it
could not explain. Both are the pattern the process document describes — a diagnostic that bounds
something before it is built is worth as much as running the thing.

Coordinator decisions on P2a's open questions:
- **The twelfth rung goes in.** `puct:eval=learned` at the baseline's own allowance, +20% per board
  and about +36 minutes total. P2a argued it and the argument is right: without it the field explains
  `chamber`'s history and is silent about `learned`'s, and `learned` is the leaf most likely to be
  adopted at P3. Paying 20% to avoid being unable to explain the headline is cheap.
- **The leaf-pair check stays documentation.** Declined for the agent's own reason, recorded in *Open
  at the close*: a bench assertion needing an unmeasured cross-machine tolerance, that fires on a busy
  machine, teaches everyone to ignore the suite.
- **`+dirty` pooling.** P2a's advice was "commit before P2b starts". **Not taken** — committing is the
  user's call, every time, and a green batch is not permission. Replaced with a check that proves the
  same thing: hash the *source* tree before block 1 and after block 4, and void the pooling if it
  moves. Amended to P2b mid-run to exclude `docs/`, since the coordinator is editing the agenda
  throughout and a whole-tree hash would have declared a false void. **The scoped hash came back
  byte-identical across the four blocks**, so the 20×20 pooling is sound, and the whole-tree hash
  moved exactly as the amendment predicted.

### P3

**P2b returned the phase pointed at the wrong bot.** Coordinator's own check before writing it up,
because the claim is large enough to deserve one: read the three `rate` tables off `.lab/` directly
rather than take the ranking on report. They say what the agent said — and one thing its summary
understated. On 8×8 and 20×20 `alphabeta:eval=territory` beats the equal-*allowance* rungs as well,
which were handed 2.5–4.6× its clock. That is a stronger claim than "wins at equal clock" and it is
the one worth quoting. Also confirmed `LearnedWeights.kt:22` from the file: the fit really is
`--rows 12 --cols 12`, and neither KDoc says the leaf is only known at that size.

The residual tables are worth reading before anybody quotes a single number from this phase. On
12×12 `alphabeta:eval=territory` scores 71% off the bare baseline where the fit expects 55%; on 8×8
`chamber`+prior scores 82% off it where the fit expects 49%, while rating ten points *below* it. The
intransitivity warning in the process document is not decoration here.

**P3 does not execute.** The standing decision pre-authorised adopting whatever P2 ranked first *for
`puct`*, and P2's answer for `puct` is "nothing". The move the number actually points at is a
different bot's default, which is new scope, so it goes to the user rather than being executed under
a pre-authorisation that does not cover it. P3a runs the verification either way: the 12×12 margin is
the narrowest (+36) and rests on an allowance that both P1 and P2a left at "≈1.0×" because it looked
like a control rather than a contender — and the field's own µs/turn column disagrees with the tape
there in the direction that matters.

**P3a inverted its own brief.** It was sent to check the narrowest margin and came back having
reversed which board is trustworthy — 8×8, the widest margin, is the one with the wrong allowance,
the worst opening coverage, and a rung that rates +131 above bare `puct` while losing its head-to-head
to it 89–111. Its closing recommendation is the one the coordinator adopted as P3b's instruction:
**adopt on candidate-versus-its-own-incumbent, not on "first in the field."** That is the difference
between a claim with disjoint intervals on two independent fields and a claim that dissolves under a
common-opponent cut.

That recommendation also means the coordinator put a question to the user on a premise P3a then
qualified. Recorded in the agenda as a blockquote under the standing decision rather than quietly
fixed, and P3b was instructed to put the 8×8 reversal in `BotLadderTest`'s own KDoc — which it did,
with the instruction not to read a regression into the top of the ladder coming apart elsewhere.

**P3b found the cost list incomplete, and the miss is structural rather than careless.** §15 was
written to cost the eval move and was read as if it costed the graduation too; six sites and a
second-order one turned up. The second-order one is the interesting one: two sentences *P1 was
dispatched to write* were made false by P3 seating the bots. A correction has a shelf life, and this
agenda has now watched one expire inside a day.

Coordinator's own check before writing P3 up, and it caught something: P3b's summary says "P4
inherits two ladder thresholds". Read `LearnedEval.kt:89` and `BotLadderTest`'s rung list directly —
the KDoc sentence is about moving `PuctBot.EVAL`'s *default*, which does now reach two rungs, whereas
`LearnedWeights.ENCODED` reaches only `eval=learned`, which after P3 is nobody's default. P4's brief
carries the correction with an instruction to verify it rather than trust either of us.

### P4

Dispatched **re-scoped**, because P2 half-falsified its thesis before it started. The phase's premise
is that train loss equalling holdout loss to five places means the fit is bounded by its features —
but that equality was measured on a 12×12 holdout from a 12×12 corpus, which makes it a statement
about *in-distribution* fit alone, and P2's −167 at 20×20 is an *out-of-distribution* failure. So the
brief puts a generalisation probe at step 0, in the process document's "probe whether the mechanism
can fire" slot, and asks the agent to report whether the binding constraint is the features or the
corpus before it adds any.

One resource the agenda could not have known about: P2 logged **39,600 matches across three board
sizes**, all searcher-against-searcher. The original corpus is 22,452 at one size.

**The step-0 probe justified its slot in twenty minutes and the phase never recovered its original
shape.** It was put there to ask whether the mechanism could fire; what it returned was that the
phase's premise was an invalid inference from a real number, and that the thing P4 was sent to add is
worth a twelfth of the thing nobody had thought to look at. The agenda has now had two phases invert
under their own step 0 — P1b's falsified instrument and this — and both times the inversion was the
phase's most valuable output. That is an argument for the slot, not a run of bad luck.

**The session was interrupted mid-phase, and what was interrupted was not what the handover
describes.** The handover covers the probe, the four features and the declined fifth, all staged and
all verified. It stops there. After writing it the agent went on to re-cost the leaf in Chrome, re-run
the browser golden suite, and start a three-board strength field — and the interruption caught the
field at 8×8 and 12×12 complete and **one of four planned 20×20 blocks**. None of it is in the
handover. So the coordinator's first job on resuming was not writing up a phase but working out from
`.lab/` timestamps and a dead process list what the phase had actually done.

Coordinator's own verification before writing any of it up:

- `jvmTest` and `ktlintCheck` green on the staged tree, before touching anything.
- Read `ChamberTree`'s flag rather than take the cost claim on report: `ChamberEval` constructs it
  without `allReadings`, so the default is off and the published `chamber` cost is genuinely
  untouched. Confirmed at the only two construction sites in the module.
- Read the golden case in full. Exactly one hash moved, it carries its own re-pin note, and
  `LearnedNet` guards the literal's shape against `PositionFeatures.LENGTH` so the two cannot drift.
- Confirmed the leaf-pair consistency check — P1's own instrument-usability gate — passes on P4's
  fresh Chrome sweep: `alphabeta` and `puct` at a shared leaf agree to 0.4–4.1% on all three boards.
- Recomputed the field's allowances from the bench rather than trusting the script: 26,268/8,751 =
  3.00× → 333, and the same at 12×12 and 20×20. They match. `chamber`'s were reused from P2 unchanged,
  correctly, since its cost did not move.

**Finishing the field was the coordinator's call and the pooling needed an argument.** Three blocks
were re-run at the same entrants, allowances and rounds, seeds 26/51/76. The `+dirty` build label
cannot distinguish the tree the first block ran at from the tree the last three did, so the P2b
protocol's substance was applied by diff instead: every source change made in between is inside a
comment, verified line by line, and outcomes are budget-deterministic rather than clock-dependent. The
pooled 4,200 matches carry 3,541 distinct games where the single surviving block carried 918.

**The field is where the loss table earned its credibility.** Step 0 predicted, in advance and on a
different instrument, that the corpus mattered four times as much at 20×20 as at the other two sizes.
The field then moved the 20×20 contrast by ~+240 and the other two by less than field composition
alone shifts a contrast. A prediction of *which board* is a much harder thing to get right by accident
than a prediction of *how much*, and it is the reason the phase's headline is stated as settled.

Two things the coordinator declined to let the phase claim. The 8×8 and 12×12 Elo moves are reported
as **not evidence** — the same-field contrasts that ought to be fixed move ±30–90 between P2's field
and P4's, and those two rows sit inside that. And the direct old-fit-against-new-fit match was never
played; the Elo case is cross-field and says so, while the loss case is same-corpus and is what the
finding rests on. Both are in *Open at the close*.

**A correction expired inside a day for the third time on this agenda, and this time the phase killed
its own.** `LearnedEval`'s *"the leaf is only known to work near 12×12"* and *"the next gain here is a
reading, not a layer"* were P2's and P3's corrections, correct when written, falsified by P4 —
alongside `EvaluationCost.LEARNED`'s cost band, `Corpus`' and `ModelScore`' present-tense references
to a shipped fit that is no longer shipped, and `docs/Bots.md`'s "twenty-five bounded numbers". The
coordinator fixed all of them, and fixed one more that no cost list would have named: **the sentence
in `docs/Workflow.md` that caused the error in the first place** — *"equal columns mean the fit is
limited by what the features can say"*, sitting in the document that teaches everyone how to read the
instrument. The stale KDoc is a consequence; that sentence was the cause, and it was the only site
where fixing it prevents a recurrence rather than recording one.

### P5

Split as the process document prescribes and for once entirely without incident: **P5a** built and
priced, **P5b** measured, and the handover between them did the job it exists for — P5b's §0 told it
in advance that an equal-allowance batch would flatter the candidate by 1.4–2.4× and would read as a
win whether or not the policy was worth anything.

**Step 0 inverted a third time, and this one is a rule rather than a result.** The agenda's own
threshold — *"changes the step in 3% of positions"* — is stated in the wrong unit, and `liberty` sits
at or under it on every board while diverging on roughly a third of *rollouts*. The candidate that
survived to be the only one worth keeping would have been killed at step 0 by this agenda's own
sentence. The firing-rate rule is sound; a denominator that is not the unit the decision is made in is
what nearly broke it.

Coordinator's own verification, and it caught something both agents had wrong:

- Confirmed the bit-identical default against `git show HEAD:` rather than on report — `uct`'s and
  `flat-monte-carlo`'s golden hashes are byte-identical to HEAD, which is the one claim the whole
  "blast radius is zero" framing rests on.
- Re-ran the probe and the bench **twice**. Every divergence figure reproduced to the digit.
- **The cost table did not.** P5a published 1.98–2.01× for `prior` at 20×20 and claimed the two larger
  boards agree to within 2% between runs; the coordinator's runs read 2.45× and then 1.93×, with the
  *control itself* swinging 11% on that board. The KDoc now carries the wider band and the reason, and
  the distinction that matters: **the allowance is asserted directly against the default's clock and
  verified to 94–113% over five runs, while the ratio is a derived number with real spread.** They are
  not reciprocals — cost per turn is a tree term plus a rollout term and only the second one carries
  the policy.
- Re-rated 12×12 and 20×20 off `.lab/` directly. Both reproduce, including the overlap on the single
  contested cell: `liberty`'s +17 at 20×20 has intervals that overlap the baseline's, which is what
  makes "level to slightly ahead" the honest phrasing rather than a win.
- Fixed a dangling citation in `RolloutPolicy`'s KDoc — it named a `RolloutDivergenceTest` that does
  not exist. P1 spent a whole phase on exactly this class of defect and it recurred inside two phases.

**P5b's incidental finding is probably worth more than P5.** `rolloutDepth=25` at the shipped
allowance beats the undepthed bot 58.5% and 67.0%, against a published 51.7% taken at a tenth of the
allowance under a KDoc sentence asserting the ratio does not turn on the allowance. The agent recorded
it as a **lead and not a finding** — naming that it is not a cost result, not a strength result and
not dismissible as noise — which is the right call and the reason it is quotable at all. It is folded
into P6's brief rather than left in *Open at the close*, because P5b's own recommendation is that P6
may close on a cost measurement in an hour, and that leaves the room to chase it.

## Decisions taken by the user

**Put on P2's number, answered before P3a returned.** Both are recorded in the agenda's
[Standing decisions](../2026-07-29_Research-Agenda.md#taken-after-p2-on-p2s-number), which is where
they belong permanently; this is the account of how they were reached.

- **Adopt `AlphaBetaBot`'s default eval `chamber` → `territory`, conditional on P3a.** The condition
  is the coordinator's, not the user's: if P3a's corrected 12×12 allowance does not hold the +36
  margin, P3b stops and reports rather than adopting. The other two boards do not need it.
- **Promote both, `alphabeta` on top.** The alternative on offer was to promote neither and say why —
  `ShippedBots.kt:69-73` explicitly permits that, and the "why" would have been that the ladder is a
  2-seat equal-allowance instrument while P2 measured at equal clock across three boards. The user
  took the evidence instead. Consequence to carry into P3b: `BotLadderTest` gains two rungs above its
  current top, so **every threshold in it is re-measured, not adjusted**, and the golden and ladder
  changes land in one phase rather than two.

### P6

Dispatched **scoped down on the previous phase's recommendation** rather than as the agenda wrote it:
one board, and the swept prior priced *first* against an explicit kill threshold the agent was told it
should want to use. It used it. An hour's measurement closed a workstream that had been carried
through two agendas, and the phase spent the rest of its session on the accident P5 had turned up.

**A kill criterion stated in the brief is worth more than a kill criterion discovered in the batch.**
P5b supplied the threshold (2.2×) and the exchange rate (82–93 Elo/e-fold) that made it computable,
and P6 could therefore close a row on a cost table rather than on a field nobody would have believed
either way. That is the cheapest phase on this agenda and it is a pattern to repeat: when a previous
phase can name the price at which the next one stops being worth running, put the number in the brief.

Coordinator's own verification: confirmed `uct`'s golden hash is still HEAD's after both P5 and P6, and
re-rated the 20×20 depth field off `.lab/` directly — `uct@760/rolloutDepth=25` at 99 (+86..+113)
against the baseline's 21 (+10..+33), disjoint as claimed, and above `puct@1k`'s 77.

Two things the agent did that the coordinator would have had to ask for otherwise. It declined to
measure the swept prior's *strength*, because a field needs a reachable entrant, a reachable entrant
needs a frozen `Choice` value, and the cost result is precisely what says not to freeze one — SW-05
reasoning applied without being prompted. And it reached for an `internal` test-only seam
(`withRolloutPolicy`) rather than either freezing an identifier or putting a second bot class at the
timed call site, which is the defect P5 had measured at 25% of a control's own reading one phase
earlier. A phase that reads the previous phase's instrument findings is the whole point of the ledger.

### P7

Split instrument-then-field, and the split earned its keep in the way the process document predicts:
P7a's handover told P7b that `ab` does not exist at three seats, that `--openings fixed` is worth
83/0/17 to a seat, and that the FFA rotation manufactures perfect intransitive cycles — three batches
P7b did not have to waste discovering.

**P7a falsified the coordinator's own brief on its first substantive point.** The brief said a 3-way
free-for-all does not produce pairwise outcomes and asked what it would take to express one.
`pairwiseOutcomes(FREE_FOR_ALL, …)` already emits `C(n,2)` per match and `fitRatings` is seat-count
agnostic — nothing needed building. The real problem was one level down and neither the agenda nor the
brief had seen it: **the rule scores outlasting, not winning.** The coordinator confirmed that from
`pairwiseOutcomes.kt:83-90` rather than on report, and it became P7b's headline question.

**Then P7b falsified P7a's design, which is the phase's most valuable output.** P7a proposed a Steiner
triple system — 12 triples covering every pair — as the covering design. P7b ran it, got two inversions
against the ladder, distrusted them, ran the complete 84-triple design, and found neither survives. A
Steiner system balances **pairs**; an outright win is a **three-way** event, so what needs balancing is
the *company*, and a pairwise cell moves up to 33.7 points on the third snake's identity alone.

That one is worth the coordinator's own verification and it got it, because it is the kind of claim a
phase could talk itself into. Both fields were re-rated off `.lab/` directly: the incomplete design
rates `chase` above `space` while it wins 11% against 20%, and `rate`'s new orderings check fires on
it; the complete design's same check passes and reproduces the whole ladder in order with win share
monotone beside it. **The defect and its absence were both watched happening rather than accepted.**

Two agents in a row now have reported the batch they wasted — P5a's falsified JVM sweep, P7b's
transposed analysis key that briefly ranked `random` second in the field and was *"caught by disbelief,
not by a test."* That is the disposition the process document asks for and it is worth recording that
it keeps showing up.

The `backup` knob was declined on a mechanism rather than on caution, which is the difference between
parking a row and closing one: no `LeafEval` returns a vector on the simplex, so the knob would move
the backup, the leaf weighting and the effective `cpuct` together — the confound relocated, not
removed. Two agendas have now parked max^n-against-paranoid; this is the first time with a reason from
the code.

### P8

Run as a single agent — the phase is one change measured two ways, not two independent halves, and the
process document's split exists for phases whose batches are long rather than for phases that are
merely important.

**The last phase of the agenda produced its most reusable rule by failing.** The fusion was built,
proven byte-identical, measured at 1.10–1.17× on the JVM and **0.50–0.91× in Chrome** — a disagreement
in *direction* on code that computes the same thing. `:bots` deploys to wasm and nowhere else, so that
is now part of SW-03: a hot-path rewrite is settled in the browser and the JVM run is the smoke test
in front of it. Every previous timing lesson on this agenda was about *precision* — min-of-passes,
boost clock, a foreign control, a derived ratio. This one is about *sign*, and it is the only one that
would have shipped a regression.

Coordinator's own verification, because "nothing shipped" is a claim that is easy to make loosely: a
diff of all three sweep sources against `HEAD` filtered to non-comment lines comes back **empty**. The
only changes are KDoc. The rule addition to `Coding-Standards.md` was read in full rather than taken on
report.

**And the agenda mis-specified the mechanism for the second phase running.** P8 was instructed to
quote the ratio per phase because `advanceAlone` "is the separated case" — it is not; it says whose
room runs out first, not whether the rooms connect. The instruction was drawn from ground truth 8,
which is about `Separation`, and the error was assuming a different predicate meant the same thing.
The instruction was still the right one to give: quoting per phase is what caught it, and the phase
effect turned out to run the *opposite* way to the one the agenda asserted.

### The 8×8 row, taken after the close

The user was put the `ROLLOUT_DEPTH` decision with three options and **chose to measure 8×8 before
deciding** rather than adopt or shelve. That was the right call and the measurement earned it: 8×8 is
a **null**, and the answer changed from *"adopt on one board and hope"* to *"this is a knob and it
should stay one."*

**The null is structural, and that is the part a strength number alone would have hidden.** At equal
allowance — the mechanism handed its clock back — the depth rung rates **78 against the baseline's
78**. Not a narrow loss to the 4–5 Elo the allowance costs; nothing at all. Per iteration the three
boards read **0 / +15 / +104**. So truncation does not merely fail to pay its way on a small board, it
has nothing to sell there, and the coordinator verified the level reading off `.lab/` directly because
it is the sentence the conclusion turns on.

The agent also declined to overwrite P6's 20×20 cost cell with its own free cross-check, on the
grounds that its 20×20 control pair swung 13–18% *within* a run — recording the instability instead of
averaging two instruments. That is the P5 lesson applied by an agent that was told about it, which is
the return on writing instrument findings into the ledger rather than into a transcript.

**Where this leaves the decision:** nowhere for the user to act. A single default cannot be right when
the value crosses zero between the two smallest boards, and a board-conditioned default is a
`:bot-api` contract change. It is the second independent argument for that change and the first with a
number attached, so it is recorded against the *Board-size-conditioned defaults* row rather than as a
pending decision.

## Decisions waiting on the user

*(none open)*

<details>
<summary>Answered — P3's adoption, as it was put</summary>

- **P3's adoption — and it is not the adoption anybody scoped.** P2 ranked
  `alphabeta:eval=territory@1000` first on all three boards at equal clock, disjoint from the
  runner-up, and ahead even of rungs handed 2.5–4.6× its wall clock on two of them. So:
  1. **`puct`'s eval default does not move** — at equal clock both alternatives are worse than the
     bare baseline (`chamber` −34/−63/−59, `learned` +40/−7/−316). The scoped adoption is a null.
  2. **The candidate is `AlphaBetaBot`'s default eval, `chamber` → `territory`**, worth +108/+46/+41
     against its own shipped leaf at equal clock. The standing decision pre-authorised adopting
     whatever P2 ranked first *for `puct`*; there is nothing there to execute, and this is new scope.
  3. **Promotion is one decision, not two.** `puct` clears `uct` by +54/+58/+62, but `alphabeta`
     clears `puct` on all three boards, so admitting `puct` on this evidence admits `alphabeta`
     above it on stronger evidence.
  P3a is verifying the 12×12 allowance the narrowest margin rests on before the decision is put.

</details>

*(Three standing decisions were already taken before the work started and are recorded in the
agenda's [Standing decisions](../2026-07-29_Research-Agenda.md#standing-decisions) rather than here.)*
